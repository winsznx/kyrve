node_modules/
dist/
out/
cache/
artifacts/
broadcast/
coverage/
typechain-types/
.turbo/
.wrangler/
*.tsbuildinfo

.env
.env.*
!.env.example

vendor/midnight/out/
vendor/midnight/cache/
spikes/**/artifacts/
spikes/**/cache/
spikes/**/types/

.DS_Store
*.log
deployments/**/.raw-deployment.json
workers/*/dist/
slither-report.json

# Brand build intermediates. brand.json is the committed lock; these are per-run scratch.
scripts/brand/build-report.json
scripts/brand/verify-report.json

# Python bytecode from the brand scripts.
__pycache__/

# Local confidential deployments are not reproducible across runs: the deployer's nonce differs
# depending on what else the suite did first, so CREATE addresses differ. The Sepolia record IS
# committed — it is a real, one-time deployment.
deployments/local/confidential.json
apps/web/public/deployment.json
evidence/phase2/confidential-suite.log
