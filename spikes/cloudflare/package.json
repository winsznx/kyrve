{
  "name": "@kyrve/spike-cloudflare",
  "private": true,
  "type": "module",
  "description": "Day 0 Spike E - proves viem, Workflows, Queues, D1 and R2 run under workerd. Not product code.",
  "scripts": {
    "dryrun": "wrangler deploy --dry-run --outdir dist",
    "test": "vitest run"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.19.0",
    "vitest": "4.1.0",
    "wrangler": "4.115.0"
  },
  "dependencies": {
    "viem": "2.55.10"
  }
}
